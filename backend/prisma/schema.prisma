generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model RegisterDetails {
  id          Int    @id @default(autoincrement())
  FirstName   String
  LastName    String
  CompanyName String
  PhoneNumber String
  Address     String
}
